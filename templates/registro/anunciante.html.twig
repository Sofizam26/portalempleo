{% extends 'registro/base.html.twig' %}

{% block title %}Registro de anunciante{% endblock %}

{% block contenido %}
<h2>Crear cuenta de anunciante</h2>

{% if error %}
    <div style="color:red;">{{ error }}</div>
{% endif %}
{% if mensaje %}
    <div style="color:green;">{{ mensaje }}</div>
{% endif %}

{{ form_start(form) }}

    {{ form_row(form.nombreAnunciante) }}
    {{ form_row(form.email) }}
    {{ form_row(form.password) }}
    {{ form_row(form.tipo) }}
    {{ form_row(form.descripcion) }}
    {{ form_row(form.sitioWeb) }}
    {{ form_row(form.ciudad) }}

    <button type="submit">Registrarse</button>

{{ form_end(form) }}

<a href="{{ path('registro_rol') }}">Volver</a>
{% endblock %}